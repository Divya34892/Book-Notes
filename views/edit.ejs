<!DOCTYPE html>
<html>
<head><title>Edit Book</title></head>
<body>
    <h1>Edit Book</h1>
    <form method="POST" action="/books/<%= book.id %>?_method=PUT">
        <label>Title: <input name="title" value="<%= book.title %>" required></label><br>
        <label>Author: <input name="author" value="<%= book.author %>" required></label><br>
        <label>Rating (1-10): <input name="rating" type="number" value="<%= book.rating %>" min="1" max="10"></label><br>
        <label>Date Read: <input name="date_read" type="date" value="<%= book.date_read.toISOString().substring(0, 10) %>"></label><br>
        <label>Cover ID (from Open Library): <input name="cover_id" value="<%= book.cover_id %>"></label><br>
        <label>Notes:<br><textarea name="notes" rows="5" cols="40"><%= book.notes %></textarea></label><br>
        <button type="submit">Update Book</button>
    </form>
    <a href="/">Back to list</a>
</body>
</html>
